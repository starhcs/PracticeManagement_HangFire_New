{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  // Local development only. Throwaway credentials for the /hangfire dashboard - this value
  // is NOT used in staging or production, where DashboardAuth__Username / DashboardAuth__Password
  // come from the environment (Key Vault / App Configuration reference).
  "DashboardAuth": {
    "Username": "hangfire",
    "Password": "LocalDev@123",
    "SessionIdleTimeoutMinutes": 5
  }
}
